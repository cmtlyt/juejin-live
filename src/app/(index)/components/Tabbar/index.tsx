import { gc } from '@cmtlyt/base';

import styles from './index.module.scss';

export function Tabbar() {
  return (
    <section className={gc([styles.tabbarWrapper, 'sticky top-0 z-10', 'bg-white'])}>123</section>
  );
}
